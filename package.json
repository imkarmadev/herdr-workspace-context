{
  "name": "herdr-workspace-context",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check src/action.mjs && node --check src/event.mjs && node --check src/dashboard.mjs && node --check src/note.mjs && node --check src/report-view.mjs",
    "test": "node --test"
  }
}
